<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/fondo">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FF6F91"
        app:title="Menu principal" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginVertical="190dp"
        android:layout_marginHorizontal="26dp"
        android:background="#C7A7A7"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="15dp">

        <!-- Título principal -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="sans-serif-medium"
            android:text="GENERADOR DE LELY-FRASES"
            android:textColor="#2F0505"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- Frase generada -->
        <TextView
            android:id="@+id/frase"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="Aquí aparecerá la frase"
            android:textColor="#000000"
            android:textSize="18sp"
            android:textStyle="italic" />

        <!-- Botón de cargar frase -->
        <Button
            android:id="@+id/btnMostrar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:backgroundTint="#FF6F91"
            android:drawablePadding="10dp"
            android:padding="10dp"
            android:text="Frase motivadora"
            android:textColor="#FFFFFF"
            android:textSize="16sp" />

        <!-- Contenedor para contraseña y botón menú -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:gravity="center"
            android:orientation="horizontal">

            <!-- Campo de contraseña -->
            <EditText
                android:id="@+id/txtContrasena"

                android:layout_width="150dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="20dp"
                android:backgroundTint="#FF6F91"
                android:hint="Contraseña"
                android:inputType="textPassword"
                android:padding="10dp"
                android:textColor="#000000"
                android:textSize="14sp" />

            <!-- Botón del menú -->
            <Button
                android:id="@+id/btnMenu"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:backgroundTint="#FF6F91"
                android:drawablePadding="10dp"
                android:padding="10dp"
                android:text="Menú frases"
                android:textColor="#FFFFFF"
                android:textSize="16sp" />
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>